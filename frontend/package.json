{
  "name": "cronos-sentinel-ai-vault-frontend",
  "version": "3.0.1",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "next build",
    "start": "(cd ../agent-service && PORT=3001 node dist/server.js &) && next start -p 3000",
    "lint": "next lint --dir .",
    "type-check": "tsc --noEmit",
    "test:build": "npm run type-check && npm run build",
    "test:all": "npm run test:build"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "ethers": "^6.16.0",
    "next": "14.2.5",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.6.3"
  }
}
