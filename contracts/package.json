{
  "name": "cronos-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "clean": "hardhat clean",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network cronosTestnet",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6",
    "@types/node": "^22.0.0",
    "chai": "^4.4.1",
    "dotenv": "^16.4.0",
    "hardhat": "^2.22.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
